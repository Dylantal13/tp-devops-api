version: '3.8'

services:
  api:
    build: 
      context: .
      dockerfile: Dockerfile
    image: api-devops:latest
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1